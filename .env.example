# ============================================
# Repo-Artist Configuration
# ============================================

# --- Required ---
# Google Gemini API Key (Required for architecture analysis)
# Get your key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# --- GitHub OAuth (Required for Web App) ---
# Create OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=http://localhost:8000/auth/callback

# --- Optional: Gemini Model ---
# Which Gemini model to use for architecture analysis
# Default: gemini-2.5-flash
ARCH_MODEL_NAME=gemini-2.5-flash

# --- Optional: Imagen 3 (Tier 1 Image Generation) ---
# Google Cloud Project ID for Imagen 3 via Vertex AI
# If not set, falls back to Tier 2 (Pollinations.ai)
# Requires: pip install google-cloud-aiplatform
# IMAGEN_PROJECT_ID=your-gcp-project-id
# IMAGEN_LOCATION=us-central1

# --- Optional: Analysis Configuration ---
# Maximum directory depth to scan (default: 3)
# REPO_ARTIST_MAX_DEPTH=3

# Maximum components to include in image prompt (default: 7)
# REPO_ARTIST_MAX_COMPONENTS=7

# Maximum connections to include in image prompt (default: 7)
# REPO_ARTIST_MAX_CONNECTIONS=7

# --- Optional: Output Configuration ---
# Output directory for generated images (default: assets)
# REPO_ARTIST_OUTPUT_DIR=assets

# --- Optional: JSON Retry Configuration ---
# Maximum retries for JSON parsing errors (default: 3)
# REPO_ARTIST_MAX_JSON_RETRIES=3
